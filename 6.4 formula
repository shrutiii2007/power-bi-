---------------
QUESTION :- 1 
---------------

ScoreCategory = 
SWITCH(
    TRUE(),
    'Students Table Q1'[Score] >= 90, "Excellent",
    'Students Table Q1'[Score]>= 75, "Good",
    'Students Table Q1'[Score] >= 50, "Average",
    "Poor"
)

---------------
QUESTION :- 2
---------------

CleanName = (TRIM('Employees Table Q2'[FullName]))

FirstName = LEFT(TRIM('Employees Table Q2'[FullName]), FIND(" ", TRIM('Employees Table Q2'[FullName])) - 1)

EmailDomain = RIGHT('Employees Table Q2'[Email], LEN('Employees Table Q2'[Email]) - FIND("@", 'Employees Table Q2'[Email]))

EmpNumber = MID('Employees Table Q2'[EmpID], 2, 3)

DeptUpper = UPPER('Employees Table Q2'[Department])

---------------
QUESTION :- 3
---------------

OrderYear = YEAR('Order Table Q3'[OrderDate])     ( -- Saal nikalo )

OrderMonth = MONTH('Order Table Q3'[OrderDate])   ( -- Mahina nikalo )

OrderDay = DAY('Order Table Q3'[OrderDate])       ( -- Din nikalo )

OrderWeekday = WEEKDAY('Order Table Q3'[OrderDate])  ( -- Weekday (1=Sunday, 7=Saturday) )

MonthYear = FORMAT('Order Table Q3'[OrderDate], "MMM-YYYY")    ( -- Month-Year text column (jaise: "Jan-2024") )

---------------
QUESTION :- 4
---------------

CustomerName = RELATED('Customers Table Q4'[CustomerName])


